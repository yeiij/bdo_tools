---
description: How to build the executable
---

To build the standalone `.exe` using PyInstaller:

```bash
make build
```
// turbo
Or:
```bash
uv run python build_exe.py
```

The output will be located in the `dist/` directory as `GameMonitor.exe`.
